<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Personal Info</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-green-800 to-gray-900 text-white min-h-screen font-sans">

  <!-- Navigation -->
  <nav
    class="flex flex-col md:flex-row md:justify-between md:items-center px-4 md:px-10 py-5 gap-4 md:gap-0 border-b border-white/20">
    <!-- Left Navigation Links -->
    <div class="flex gap-4 md:gap-10 text-sm md:text-lg font-medium justify-center items-center">
      <a href="/profile" class="border-b-2 border-white pb-1">Personal Info</a>
      <a href="/candidates" class="border-b-2 border-transparent hover:border-white transition">Candidates</a>
      <a href="/vote/counts" class="border-b-2 border-transparent hover:border-white transition">Vote Counts</a>
      <% if (user.role==="voter" ) { %>
        <a href="/vote/panel" class="border-b-2 border-transparent hover:border-white transition">Voting Panel</a>
        <% } %>
    </div>

    <!-- Right Buttons -->
    <div class="flex items-center gap-4 justify-center">
      <% if (user.role==="admin" ) { %>
        <a href="/candidates/create"
          class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-sm md:text-base transition">Create Candidate</a>
        <% } %>


          <!-- Logout Button -->
          <form action="/logout" method="POST">
            <button type="submit" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded text-sm md:text-base transition">
              Logout
            </button>
          </form>
    </div>
  </nav>

  <!-- Profile Info Section -->
  <section class="flex flex-col items-center mt-10 px-4">
    <div class="bg-white rounded-full p-2 w-40 h-40 flex items-center justify-center">
      <img src="/images/user.png" alt="Profile Picture" class="w-36 h-36 rounded-full object-cover">
    </div>
    <button class="mt-4 bg-blue-600 hover:bg-blue-700 px-6 py-2 rounded text-sm md:text-base transition">Change Profile
      Picture</button>
    <a href="/profile/update-password" class="mt-4 flex items-center gap-2 text-white hover:text-blue-300 transition">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24"
        stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
          d="M15.232 5.232l3.536 3.536M9 11l6-6m2 2l-6 6M5 13l6 6m0 0h4a2 2 0 002-2v-4m-6 6l-6-6" />
      </svg>
      <span>Edit Password</span>
    </a>
  </section>

  <!-- User Details -->
  <section class="mt-10 max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-6 px-4 text-base">
    <div>
      <p><span class="text-blue-400 font-semibold">Name:</span>
        <%= user.name %>
      </p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Age:</span>
        <%= user.age %>
      </p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Email:</span>
        <%= user.email %>
      </p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Aadhar Number:</span>
        <%= user.aadharCardNumber %>
      </p>
    </div>
    <div>
      <p><span class="text-blue-400 font-semibold">Mobile Number:</span>
        <%= user.mobile %>
      </p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Address:</span> XYZ, Street No. 92, Gurgaon, Uttar
        Pradesh, 100021</p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Eligible:</span> True</p>
      <p class="mt-2"><span class="text-blue-400 font-semibold">Role:</span>
        <%= user.role %>
      </p>
    </div>
  </section>

  <!-- Footer -->
  <footer class="text-center mt-16 text-sm text-gray-400 px-4 pb-6">
    <p>This is a secure and verified profile page for registered voters and officials.</p>
    <p class="mt-1">© <%= new Date().getFullYear() %> National Online Voting System</p>
  </footer>

</body>

</html>